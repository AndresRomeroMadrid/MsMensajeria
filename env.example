PORT=
DB_USER=
DB_PASSWORD=
#db si es docker
DB_HOST=
DB_PORT=
DB_DATABASE=
UPLOAD_PATH=
API_RESEND=
DB_SSL=
JWT_SECRET=

# dev | prod — controla si se usa RabbitMQ (dev) o SQS (prod)
ENVIRONMENT=dev

# RabbitMQ (solo ENVIRONMENT=dev)
RABBITMQ_URL=

# AWS SQS (solo ENVIRONMENT=prod)
SQS_QUEUE_URL=https://sqs.<region>.amazonaws.com/<account-id>/gestion-queue
AWS_REGION=us-east-1
